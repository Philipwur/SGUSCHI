# To-Do

## High Priority

- Remove need for master submission script. Instead create master SGUSCHI process.
  + Master SGUSCHI should generate/manage the scheduler job script instead of requiring a manually customised jobsub/OxidationMaster.
  + Add a scheduler-neutral job wrapper around the VASP command, generated by Master SGUSCHI rather than hand-edited by users, which writes job.started, job.exit, job.killed, and optionally job.heartbeat in each Dir_VolSearch folder.
  + SimulationSummary should read those wrapper markers before falling back to file timestamps, so started-but-killed jobs can become FAILED/STALE instead of UNKNOWN without depending on Slurm/PBS commands.
  + Use job.started/job.exit marker times to report total VASP job runtime independently of Slurm/PBS accounting; reserve scheduler-specific accounting for optional CPU/GPU-hour reporting.
- Remove need for INCAR (and maybe also job.in) in starting folder. Instead have saved template.
- Set time limit variable.
- Add all atomic weights and see if we can make the script more friendly to other materials systems.

## README

- Add TestCase to README.

## Postprocessing

- Add Pressure method to postprocessing from DevArea.

## OxidationStep

- Fix indexing checks on OxidationStep. Current 'expecting to read print' is correct. Make check more thorough somehow.
  + VolSearch requires a folder where all old sims are in their folders, and new sim has yet to be moved into new folder.
  + Look for error file (if job fails), restart job with a bit of extra time (keep track somehow, make new Python workflow for adding some extra time relative to last attempted) and remove error file. Once this failed job is done, go back to originally given time.
- Create a better logging system in RootDir (some kind of table which gets updated).
- Maybe also log CPU hours / GPU hours per job (make it optional in case job is not run on Slurm).
- Add check for radii, POTCAR, POSCAR cohesion (this can only work if runtime is VASP).
- Add more runtime checks to ensure things are running smoothly.
- Elemental mass dictionary only covers O, C, Zr, N; additional elements must be added manually to `OxidationAnalysis.py`.

## SLUSCHI Source

- Change .sluschi.rc / make so that there is a sluschipath2/sguschipath, then you can install both SLUSCHI and SGUSCHI without conflict. Make sure all references to sluschipath are changed to sluschipath2 or sguschipath.

## PrepareWorkPlace

- Check POTCAR, POSCAR, CovalentRadii to see that they all match.
- Run the first job so volsearch_cont works.
- Read job.in for job submission command.
- Create template OxidationMaster (see template).
- Make job.in steps 10000000.
- Add error file if job fails in jobsub.

## Testing

- Snapshot (golden-file) tests for VaspIO parsers using real OUTCAR/POSCAR files trimmed to a few ionic steps — covers real atom counts and element ordering that synthetic 3-atom fixtures miss.
- End-to-end OxidationStep smoke test pointing at a trimmed real example directory, run in TestCase=True mode, asserting no exceptions and spot-checking key outputs (GasFraction in [0,1], atom count stable when no O2 added, etc.).
- Store trimmed fixture files (3-frame OUTCAR, real POSCAR) in src/test/fixtures/.

## utils/Rollback.py

- Change Rollback to use last step of OUTCAR, so rollback can handle cases where gases were removed/added.

## OxidationPreProcessing

- Add supercell generation.
- Add SQS (read paper first).
- Add thermal expansion section.

## LAMMPS Interface

- Add interface for LAMMPS as an alternative MD backend to VASP.

## Misc

- Re-do GPU container to include pmi2 so it can be spread across nodes.
